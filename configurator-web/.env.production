# URL развёрнутого runner-web для embedded live-preview iframe.
# Конфигуратор и runner раздаются с одного origin (8080), runner — корень,
# конфигуратор — /configurator/. Поэтому достаточно относительной ссылки.
VITE_RUNNER_EMBED_URL=/
