{
  "dslVersion": "1.0",
  "metadata": {
    "name": "[A4] Библиотечный 'Связь со старшим' — версия 2",
    "description": "Версия 2: добавили 'Видеозвонок' и 'Корпоративный мессенджер'. Существующие родители на v1 это изменение НЕ ВИДЯТ автоматически (см. решение Р4: pin к версии). Новые родители могут явно сослаться на version=2.",
    "scenarioGuid": "00000000-0000-0001-0000-000000000020",
    "version": 2,
    "kind": "library",
    "purpose": "architecture-demo",
    "changesSinceV1": [
      "Добавлена опция 'video_call' в method",
      "Добавлена опция 'messenger' в method"
    ]
  },
  "initialStepId": "method",
  "steps": [
    {
      "id": "method",
      "type": "RadioButton",
      "view": {
        "label": "Способ связи со старшим смены",
        "options": [
          { "id": "phone",      "label": "Телефон" },
          { "id": "in_person",  "label": "Лично"   },
          { "id": "video_call", "label": "Видеозвонок" },
          { "id": "messenger",  "label": "Корпоративный мессенджер" },
          { "id": "no_reply",   "label": "Не отвечает" }
        ],
        "required": true
      },
      "transitions": { "default": { "goto": "decision" } }
    },
    {
      "id": "decision",
      "type": "Comment",
      "view": {
        "label": "Указания старшего",
        "required": true,
        "minLength": 10
      },
      "transitions": { "default": { "goto": "ack" } }
    },
    {
      "id": "ack",
      "type": "Button",
      "view": { "label": "Связь зафиксирована" },
      "editable": false,
      "transitions": { "default": { "actions": [ { "type": "finish" } ] } }
    }
  ]
}
