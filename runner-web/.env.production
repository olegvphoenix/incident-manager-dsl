# URL развёрнутого configurator-web для перехода из runner-web.
# Конфигуратор раздаётся как подпуть /configurator/ внутри того же runner-web
# контейнера на 8080 — поэтому используем относительный путь.
VITE_CONFIGURATOR_URL=/configurator/
